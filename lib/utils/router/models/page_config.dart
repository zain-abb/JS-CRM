import '../../constants/app_paths.dart';
import '../pages.dart';

PageConfiguration splashPageConfig = const PageConfiguration(key: 'Splash', path: splashPath, uiPage: Pages.splashPage);
PageConfiguration loginPageConfig = const PageConfiguration(key: 'Login', path: loginPath, uiPage: Pages.loginPage);
